# Usage Notes

- This library expects Tailwind CSS to be active in your app and Ocean Professional CSS variables declared in global styles.
- Include `primeicons/primeicons.css` in your styles array (already configured).
- Provide icons via component `iconUrl` inputs, typically from `assets/figmaimages/`.
